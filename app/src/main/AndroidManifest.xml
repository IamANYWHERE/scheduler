<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.toplyh.android.scheduler">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
        <activity android:name=".ui.test.RegistActivity">
        </activity>
        -->
        <activity android:name=".ui.test.NoteActivity">
        </activity>
        <activity android:name=".ui.test.ReactiveNoteActivity">
        </activity>
        <activity android:name=".ui.test.LoginActivity">
        </activity>
        <activity android:name=".ui.test.NettyActivity">
        </activity>
        <activity android:name=".ui.test.WebSocketToAllActivity">
        </activity>
        <activity android:name=".ui.test.CheatActivity">
        </activity>
        <activity android:name=".ui.test.StompActivity">
        </activity>
        <activity android:name=".ui.activity.MaterialLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.MaterialRegistActivity">
        </activity>
    </application>

</manifest>